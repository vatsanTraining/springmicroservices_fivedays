spring:
  application:
    name: tour-rest-api
        
        
          cloud:
    config:
      fail-fast: true
      discovery:
        service-id: config-server
    discovery:
      enabled: true
     
      