spring:
  application:
    name: tour-agent-rest-api
        
        
          cloud:    config:      fail-fast: true
      retry:        max-attempts: 15
        max-interval: 120000      discovery:        service-id: config-server
    discovery:      enabled: true
      
      
      